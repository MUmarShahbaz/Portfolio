<object data="{{ include.pdf }}" type="application/pdf" width="100%" height="600px">
    <p>Your browser doesn't support embedded PDFs. 
       <a href="{{ include.pdf }}" class="btn btn-primary">Download the PDF</a> instead.</p>
</object>