function showNextSlide(e){images[currentIndex].classList.remove("active"),images[e].classList.add("active");const t=images[e];titleElement&&(titleElement.textContent=t.getAttribute("data-title"),descriptionElement.textContent=t.getAttribute("data-description"))}function autoSlide(){showNextSlide((currentIndex+1)%images.length),currentIndex=(currentIndex+1)%images.length}const images=document.querySelectorAll(".slide-image"),titleElement=document.getElementById("slide-title"),descriptionElement=document.getElementById("slide-description"),prevBtn=document.querySelector(".prev-btn"),nextBtn=document.querySelector(".next-btn");let autoSlideTimer,currentIndex=0;autoSlideTimer=setInterval(autoSlide,5e3),prevBtn.addEventListener("click",(()=>{showNextSlide((currentIndex-1+images.length)%images.length),currentIndex=(currentIndex-1+images.length)%images.length,clearInterval(autoSlideTimer),autoSlideTimer=setInterval(autoSlide,5e3)})),nextBtn.addEventListener("click",(()=>{showNextSlide((currentIndex+1)%images.length),currentIndex=(currentIndex+1)%images.length,clearInterval(autoSlideTimer),autoSlideTimer=setInterval(autoSlide,5e3)}));